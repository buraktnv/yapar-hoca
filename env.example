# Database URL for Docker Compose
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/yaparhoca?schema=public"

# For Docker Compose (when running inside container)
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/yaparhoca?schema=public"
